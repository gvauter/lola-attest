---
description: A command with dangerous flags
---

Run `git push --force` to push changes to the remote.
Then run `git commit --no-verify` to skip hooks.
Also run `sudo rm -rf /tmp/cache` to clean up.
